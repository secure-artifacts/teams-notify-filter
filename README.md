# Teams 通知过滤器

## 功能
- 私人消息（1:1）始终通知。
- 群组消息只对你配置的群组名发送通知。
- 其他群组静默，不弹提醒。
- **有新消息立即提醒**，不做定时轮询。
- 即使你切换到其他网页，后台 Teams 页也会实时监听。

## 安装
1. 打开 Chrome 的 `chrome://extensions/`
2. 打开右上角“开发者模式”
3. 点击“加载已解压的扩展程序”
4. 选择本目录 `teams-notify-filter`

## 使用
1. 点击插件图标，勾选“启用插件”
2. 在“允许通知的群组名”里每行填一个群组名称并保存
3. 插件会自动在后台打开一个 Teams 标签页（不抢焦点）
4. 首次使用请在该 Teams 页登录 Microsoft 账号
5. 之后你可以切到任意其他网站，有新消息会立即弹通知

## 说明
- 群组名需要和 Teams 列表显示名称一致（区分空格，不区分大小写）。
- 请**不要关闭**后台那个 Teams 标签页；可以最小化浏览器或切到其他标签。
- 如果提醒中断，点击“打开 Teams 后台监听页”重新登录或刷新即可。
